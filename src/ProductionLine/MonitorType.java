package ProductionLine;

/**
 * The production facility will also create portable movie players. The main difference between
 * these and the audio players is that they contain screens. Create an enum called MonitorType that
 * will store the types: LCD and LED.
 */
public enum MonitorType {
  LCD,
  LED;
}
